# @package _global_
defaults:
  - override /hydra/launcher: joblib
  - override /data: full-doc

task_name: midv_baseline_fulldoc_${paths.split_name}
seed: 0


task: 2

hydra:
  launcher:
    # override the number of jobs for joblib
    n_jobs: 24